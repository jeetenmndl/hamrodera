import React from "react";

export const metadata = {
  title: "Post room",
  description: "Post the details of the room you want to sell. You may need to enter location, owner info, contact info, rent, rooms, facilities, rules and photos.",
};


export default function PostRoomLayout({ children }) {
    return (
    <> 
      {children}
    </>
  )
}